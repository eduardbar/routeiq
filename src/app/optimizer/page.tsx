import { OptimizerView } from "@/components/optimizer/optimizer-view";

export const metadata = {
  title: "Optimizer — RouteIQ",
  description: "AI-powered LLM routing optimization suggestions",
};

export default function OptimizerPage() {
  return <OptimizerView />;
}
